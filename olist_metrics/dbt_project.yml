
name: 'olist_metrics'
version: '1.0.0'
profile: 'olist_metrics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:         # directories to be removed by `dbt clean`
  - "target"
  - "dbt_packages"

models:
  olist_metrics:
    staging:
      +materialized: view
    intermediate:
      +materialized: view
    marts:
      core:
        +materialized: table 
      finance:
        +materialized: table
      marketing:
        +materialized: table
      operations:
        +materialized: table 
      product:
        +materialized: table 
      ml:
        +materialized: table 
